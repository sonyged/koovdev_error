{
  "name": "koovdev_error",
  "version": "0.0.1",
  "description": "error handling utilities",
  "main": "koovdev_error.js",
  "scripts": {
    "test": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha -- --recursive -R spec -r should",
    "posttest": "./node_modules/.bin/istanbul check-coverage && rm -rf coverage"
  },
  "devDependencies": {
    "mocha": "latest",
    "istanbul": "latest",
    "should": "latest"
  },
  "author": "",
  "license": "ISC"
}
